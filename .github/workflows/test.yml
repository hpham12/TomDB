name: Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    container:
      image: hpham99/tomdb:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure
        run: cmake -B build-coverage -DENABLE_COVERAGE=ON

      - name: Build and Test
        run: cmake --build build-coverage --target coverage